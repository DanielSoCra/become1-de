---
import DashboardMockup from '../ui/DashboardMockup.astro';
import MobileAppMockup from '../ui/MobileAppMockup.astro';

const base = import.meta.env.BASE_URL;
---

<section class="bg-[#FAF9F7] pt-16 pb-8 md:pt-24 md:pb-16 overflow-hidden relative">
  <!-- Subtle dot grid background -->
  <div class="absolute inset-0 pointer-events-none" style="background-image:radial-gradient(circle, rgba(26,107,74,0.12) 1px, transparent 1px);background-size:32px 32px;opacity:0.4;"></div>
  <!-- Radial glow top-right -->
  <div class="absolute top-0 right-0 w-[700px] h-[700px] rounded-full pointer-events-none" style="background:radial-gradient(circle, rgba(26,107,74,0.07) 0%, transparent 65%);transform:translate(25%,-25%);"></div>

  <div class="container mx-auto px-6 max-w-7xl relative z-10">
    <div class="grid lg:grid-cols-2 gap-10 lg:gap-12 items-center">

      <!-- ── Left: Copy ── -->
      <div class="space-y-6 max-w-xl">
        <div class="flex items-center gap-3">
          <span class="inline-flex items-center gap-1.5 bg-[#1A6B4A]/10 text-[#1A6B4A] text-xs font-bold px-3 py-1.5 rounded-full border border-[#1A6B4A]/20">
            <span class="w-2 h-2 rounded-full bg-[#1A6B4A] animate-pulse"></span>
            OMR Top 100 Tool 2024
          </span>
        </div>

        <h1 class="text-4xl md:text-5xl lg:text-[56px] font-extrabold leading-[1.05] tracking-tight text-[#1B1D1E]">
          Mehr Netto für dein Team.<br class="hidden md:block" />
          <span class="text-[#1A6B4A]">Weniger Aufwand</span> für HR.
        </h1>
        <p class="text-lg text-[#1B1D1E]/70 leading-relaxed">
          become.1 ist die All-in-One Plattform für steuerfreie Mitarbeiter-Benefits. Vollautomatisch, rechtssicher und in 20 Minuten einsatzbereit.
        </p>

        <div class="flex flex-wrap items-center gap-4 text-sm text-[#1B1D1E]/50">
          <div class="flex items-center gap-1.5">
            <svg xmlns="http://www.w3.org/2000/svg" class="h-4 w-4 text-[#1A6B4A]" fill="none" viewBox="0 0 24 24" stroke="currentColor"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m5.618-4.016A11.955 11.955 0 0112 2.944a11.955 11.955 0 01-8.618 3.04A12.02 12.02 0 003 9c0 5.591 3.824 10.29 9 11.622 5.176-1.332 9-6.03 9-11.622 0-1.042-.133-2.052-.382-3.016z" /></svg>
            DSGVO-konform
          </div>
          <div class="flex items-center gap-1.5">
            <svg xmlns="http://www.w3.org/2000/svg" class="h-4 w-4 text-[#1A6B4A]" fill="none" viewBox="0 0 24 24" stroke="currentColor"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 15v2m-6 4h12a2 2 0 002-2v-6a2 2 0 00-2-2H6a2 2 0 00-2 2v6a2 2 0 002 2zm10-10V7a4 4 0 00-8 0v4h8z" /></svg>
            ISO 27001
          </div>
          <div class="flex items-center gap-1.5">
            <svg xmlns="http://www.w3.org/2000/svg" class="h-4 w-4 text-[#1A6B4A]" fill="none" viewBox="0 0 24 24" stroke="currentColor"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 20h5v-2a3 3 0 00-5.356-1.857M17 20H7m10 0v-2c0-.656-.126-1.283-.356-1.857M7 20H2v-2a3 3 0 015.356-1.857M7 20v-2c0-.656.126-1.283.356-1.857m0 0a5.002 5.002 0 019.288 0M15 7a3 3 0 11-6 0 3 3 0 016 0z" /></svg>
            1.000+ Unternehmen
          </div>
        </div>

        <div class="flex flex-col sm:flex-row items-start sm:items-center gap-4 pt-2">
          <a href={`${base}demo/`} class="btn-primary">Kostenlose Demo vereinbaren</a>
          <a href={`${base}preise/`} class="btn-ghost">
            Preise ansehen
            <svg xmlns="http://www.w3.org/2000/svg" class="h-4 w-4" fill="none" viewBox="0 0 24 24" stroke="currentColor"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7" /></svg>
          </a>
        </div>

        <div class="flex items-center gap-6 pt-2 border-t border-[#F0EDE8]">
          <div>
            <p class="text-2xl font-extrabold text-[#1B1D1E]">Ø 1.200 €</p>
            <p class="text-xs text-[#1B1D1E]/50">Netto-Vorteil / MA / Jahr</p>
          </div>
          <div class="w-px h-10 bg-[#F0EDE8]"></div>
          <div>
            <p class="text-2xl font-extrabold text-[#1B1D1E]">20 Min.</p>
            <p class="text-xs text-[#1B1D1E]/50">Setup-Zeit</p>
          </div>
          <div class="w-px h-10 bg-[#F0EDE8]"></div>
          <div>
            <p class="text-2xl font-extrabold text-[#1B1D1E]">40+</p>
            <p class="text-xs text-[#1B1D1E]/50">Integrationen</p>
          </div>
        </div>
      </div>

      <!-- ── Right: Mockup stage ── -->
      <div class="mockup-stage hidden lg:block">

        <!-- Ambient glow -->
        <div class="absolute inset-0 pointer-events-none" style="background:radial-gradient(ellipse 90% 80% at 55% 50%, rgba(26,107,74,0.08) 0%, transparent 70%);"></div>

        <!-- Rating badge — top right, above everything -->
        <div class="badge-rating">
          <p style="font-size:22px;font-weight:800;line-height:1;">4,9 ★</p>
          <p style="font-size:11px;font-weight:600;margin-top:2px;">Kundenbewertung</p>
        </div>

        <!-- Live indicator -->
        <div class="badge-live">
          <span class="w-2 h-2 rounded-full bg-[#1A6B4A] animate-pulse flex-shrink-0"></span>
          <span class="text-xs font-semibold text-[#1B1D1E]">91% Aktivierungsrate</span>
        </div>

        <!-- Dashboard — scaled to fit the stage width, slight tilt -->
        <div class="dashboard-container">
          <DashboardMockup />
        </div>

        <!-- Mobile phone — overlaid bottom-left -->
        <div class="mobile-container">
          <MobileAppMockup />
        </div>

      </div>

      <!-- Mobile-only: dashboard only, no overlap -->
      <div class="lg:hidden mt-8">
        <div style="transform:scale(0.85);transform-origin:top left;">
          <DashboardMockup />
        </div>
      </div>

    </div>
  </div>
</section>

<style>
  .mockup-stage {
    position: relative;
    height: 480px;
  }

  /* Dashboard: fills the right half, scaled down to ~70% to fit */
  .dashboard-container {
    position: absolute;
    top: 10px;
    right: -10px;
    left: 60px;
    /* Scale the 760px dashboard to fit the available ~530px width */
    transform: perspective(1600px) rotateY(-5deg) rotateX(2deg) scale(0.68);
    transform-origin: top right;
    overflow: hidden;
  }

  /* Mobile: positioned bottom-left, in front of dashboard */
  .mobile-container {
    position: absolute;
    bottom: -20px;
    left: -10px;
    z-index: 20;
    transform: perspective(900px) rotateY(6deg) rotateX(-2deg);
    transform-origin: bottom left;
    filter: drop-shadow(0 24px 48px rgba(0,0,0,0.25));
  }

  .badge-rating {
    position: absolute;
    top: -4px;
    right: 4px;
    z-index: 30;
    background: #F5A623;
    color: #1B1D1E;
    border-radius: 16px;
    padding: 10px 16px;
    box-shadow: 0 8px 24px rgba(245,166,35,0.35);
    transform: rotate(2deg);
  }

  .badge-live {
    position: absolute;
    top: 60px;
    right: 0;
    z-index: 30;
    display: flex;
    align-items: center;
    gap: 8px;
    background: white;
    border-radius: 12px;
    padding: 8px 12px;
    box-shadow: 0 4px 16px rgba(0,0,0,0.1);
    border: 1px solid #F0EDE8;
  }
</style>
