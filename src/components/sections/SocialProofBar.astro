---
const companies = ['Trade Republic', 'Hypoport', 'Stadler', 'VHP', 'Optik Hallmann', 'Proks'];
---

<section class="bg-[#F0EDE8] py-12 overflow-hidden">
  <div class="container mx-auto px-6 max-w-7xl">
    <p class="text-center text-sm font-semibold text-[#4A5568] mb-8 uppercase tracking-wider">
      Über 1.000 Unternehmen in Deutschland vertrauen auf become.1
    </p>
    <div class="flex items-center justify-center flex-wrap gap-x-12 gap-y-6">
      {companies.map((name) => (
        <span class="text-lg font-bold text-[#4A5568] tracking-tight">{name}</span>
      ))}
    </div>
  </div>
</section>
